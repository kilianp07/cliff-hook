# cliff-hook

## Usage
Add the following to your `.pre-commit-config.yaml` file:

```yaml
    - repo: https://github.com/kilianp07/cliff-hook
      rev: v1.0.1
      hooks:
          - id: git-cliff
```
